<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}My Awsome Blog!{% endblock %}</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">
    {% block stylesheets %}{% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('homepage') }}">GIF Blog</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">Accueil</a></li>
                <li><a href="{{ path('admin_add_post') }}">Ajouter un GIF</a></li>
            </ul>

            {#<form method="get" action="{{ path('search_post') }}" class="navbar-form navbar-left" role="search">#}
                {#<div class="form-group">#}
                    {#<input type="text" name="search" id="search" class="form-control">#}
                {#</div>#}
                {#<button type="submit" class="btn btn-default">Rechercher</button>#}
            {#</form>#}

            {% if app.user %}
                <ul class="nav navbar-nav">
                    <li><a href="">Se déconnecter</a></li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>
<div class="container">
    {% block body %}{% endblock %}
    {% block javascripts %}{% endblock %}
</div>
</body>
</html>
